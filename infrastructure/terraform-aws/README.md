# AWS modules (ECS Fargate + RDS + VPC)
Lifted from the mitch-ai-platform monorepo. Pairs with `.github/workflows/deploy-aws.yml` and `infrastructure/aws/task-definition.json` (ECS Fargate, ECR, ALB, RDS PostgreSQL, Secrets Manager, eu-west-2). Last successful deploy run: 2026-02-12 (workflow "Deploy to AWS").
